{
  "name": "jobflow-executor-e2e",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "implicitDependencies": ["jobflow-executor"],
  "targets": {
    "e2e": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{e2eProjectRoot}"],
      "options": {
        "jestConfig": "apps/jobflow-executor-e2e/jest.config.cts",
        "passWithNoTests": true
      },
      "dependsOn": ["jobflow-executor:build", "jobflow-executor:serve"]
    }
  }
}
