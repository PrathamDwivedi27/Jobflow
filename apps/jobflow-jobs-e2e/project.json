{
  "name": "jobflow-jobs-e2e",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "implicitDependencies": ["jobflow-jobs"],
  "targets": {
    "e2e": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{e2eProjectRoot}"],
      "options": {
        "jestConfig": "apps/jobflow-jobs-e2e/jest.config.cts",
        "passWithNoTests": true
      },
      "dependsOn": ["jobflow-jobs:build", "jobflow-jobs:serve"]
    }
  }
}
